{
  "scripts": {
    "test": "jest",
    "lint": "eslint --ignore-pate .gitignore .",
    "build": "webpack --mode=production",
    "validate": "npm run lint && npm run test && npm run build",
    "setup": "npm install && run validate"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.19.1",
    "jest": "^29.0.3",
    "jest-environment-jsdom": "^29.0.3"
  }
}
